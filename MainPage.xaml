<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             xmlns:shimmer="clr-namespace:Syncfusion.Maui.Toolkit.Shimmer;assembly=Syncfusion.Maui.Toolkit"
			 xmlns:vms="clr-namespace:Cash_Tracker.Resources.ViewModel"
			 xmlns:local="clr-namespace:Cash_Tracker"
			 x:Class="Cash_Tracker.MainPage">

    <ScrollView>
        <shimmer:SfShimmer x:Name="Shimmer" WaveDirection="Default" BackgroundColor="{StaticResource OffBlack}" WaveColor="{StaticResource Gray500}" Fill="{StaticResource Gray600}">
            <shimmer:SfShimmer.CustomView>
                <VerticalStackLayout>
                    <StackLayout HorizontalOptions="Center" Margin="25" Spacing="0">
                        <BoxView BackgroundColor="{StaticResource Gray600}" HeightRequest="35" CornerRadius="7" WidthRequest="350"/>
                    </StackLayout>

                    <StackLayout HorizontalOptions="Center" Margin="10,25" Spacing="20">
                        <BoxView BackgroundColor="{StaticResource Gray600}" HeightRequest="22" CornerRadius="5" WidthRequest="150"/>
                        <shimmer:ShimmerView BackgroundColor="{StaticResource Gray600}" HeightRequest="300" ShapeType="Circle"/>
                        <BoxView BackgroundColor="{StaticResource Gray600}" HeightRequest="15" CornerRadius="3" WidthRequest="100"/>
                    </StackLayout>

                    <StackLayout HorizontalOptions="Center" Margin="10,25" Spacing="20">
                        <BoxView BackgroundColor="{StaticResource Gray600}" HeightRequest="22" CornerRadius="5" WidthRequest="150"/>
                        <shimmer:ShimmerView BackgroundColor="{StaticResource Gray600}" HeightRequest="300" ShapeType="Circle"/>
                        <BoxView BackgroundColor="{StaticResource Gray600}" HeightRequest="15" CornerRadius="3" WidthRequest="100"/>
                    </StackLayout>

                    <StackLayout HorizontalOptions="Center" Margin="10,25" Spacing="20">
                        <BoxView BackgroundColor="{StaticResource Gray600}" HeightRequest="300" CornerRadius="20" WidthRequest="350"/>
                    </StackLayout>
                </VerticalStackLayout>
            </shimmer:SfShimmer.CustomView>

            <VerticalStackLayout x:Name="Obsah" IsVisible="False">
                <StackLayout HorizontalOptions="Center" Margin="25">
                    <Label Text="Aktuální zůstatek: " x:Name="LblZustatek" FontSize="25" FontAttributes="Bold" TextColor="WhiteSmoke"/>
                </StackLayout>

                <StackLayout HorizontalOptions="Center" Margin="10,25" Spacing="20">
                    <Label Text="Příjmy podle kategorií:" HorizontalOptions="Center" FontSize="17"/>
                    <lvc:PieChart x:Name="PieChartPrijmy" HeightRequest="300" BackgroundColor="{StaticResource OffBlack}" LegendPosition="Bottom" AnimationsSpeed="00:00:02">
                        <lvc:PieChart.SeriesTemplate>
                            <DataTemplate x:DataType="vms:PieChartData">
                                <lvc:XamlPieSeries SeriesName="{Binding Kategorie}" Values="{Binding Castky}" MaxRadialColumnWidth="60"/>
                            </DataTemplate>
                        </lvc:PieChart.SeriesTemplate>
                    </lvc:PieChart>
                </StackLayout>

                <StackLayout HorizontalOptions="Center" Margin="10,25" Spacing="20">
                    <Label Text="Výdaje podle kategorií:" HorizontalOptions="Center" FontSize="17"/>
                    <lvc:PieChart x:Name="PieChartVydaje" HeightRequest="300" BackgroundColor="{StaticResource OffBlack}" LegendPosition="Bottom" AnimationsSpeed="00:00:02">
                        <lvc:PieChart.SeriesTemplate>
                            <DataTemplate x:DataType="vms:PieChartData">
                                <lvc:XamlPieSeries SeriesName="{Binding Kategorie}" Values="{Binding Castky}" MaxRadialColumnWidth="60"/>
                            </DataTemplate>
                        </lvc:PieChart.SeriesTemplate>
                    </lvc:PieChart>
                </StackLayout>

                <StackLayout HorizontalOptions="Center" Margin="10,25" x:DataType="local:MainPage">
                    <lvc:CartesianChart x:Name="GrafCelkovaCastka" HeightRequest="300" Background="{StaticResource OffBlack}" EasingFunction="{x:Null}">
                        <lvc:CartesianChart.XAxes>
                            <lvc:AxesCollection>
                                <lvc:XamlDateTimeAxis DateFormatter="{Binding Formatter}" Interval="1.00:00:00"/>
                            </lvc:AxesCollection>
                        </lvc:CartesianChart.XAxes>
                    </lvc:CartesianChart>
                </StackLayout>
            </VerticalStackLayout>
        </shimmer:SfShimmer>
    </ScrollView>
</ContentPage>